# PostgreSQL Configuration
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=your_password
DB_NAME=cowfit_db

# Alternatively, you can use a single connection string:
# DB_URL=postgres://user:password@localhost:5432/cowfit_db

# Set to 'true' if your PostgreSQL server requires SSL (e.g., Supabase, Neon)
DB_SSL=false
